# Copy to .env and fill in
OPENAI_API_KEY=
DATABASE_URL=

# Optional. Raises the openFDA rate limit from 40 req/min (unauthenticated)
# to 240 req/min. Ingestion works fine without it. Get one free at
# https://open.fda.gov/apis/authentication/
OPENFDA_API_KEY=
